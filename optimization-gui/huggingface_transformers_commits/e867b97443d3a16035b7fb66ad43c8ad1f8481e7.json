{
    "author": "Cyrilvallez",
    "message": "Deterministic sorting in modular converter when adding new functions (#35795)\n\ndeterministic sort",
    "sha": "e867b97443d3a16035b7fb66ad43c8ad1f8481e7",
    "files": [
        {
            "sha": "e75393364ff0485566c7f3220f59ad4ba0743718",
            "filename": "utils/modular_model_converter.py",
            "status": "modified",
            "additions": 6,
            "deletions": 0,
            "changes": 6,
            "blob_url": "https://github.com/huggingface/transformers/blob/e867b97443d3a16035b7fb66ad43c8ad1f8481e7/utils%2Fmodular_model_converter.py",
            "raw_url": "https://github.com/huggingface/transformers/raw/e867b97443d3a16035b7fb66ad43c8ad1f8481e7/utils%2Fmodular_model_converter.py",
            "contents_url": "https://api.github.com/repos/huggingface/transformers/contents/utils%2Fmodular_model_converter.py?ref=e867b97443d3a16035b7fb66ad43c8ad1f8481e7",
            "patch": "@@ -773,6 +773,9 @@ def compute_relative_order(self, missing_dependencies: set[str]) -> dict[str, in\n                     original_dependencies.append(class_dep)\n                 else:\n                     merged_dependencies.append(class_dep)\n+            # We need to sort deterministically before actual sorting, so that entries missing (i.e. with value 1e10)\n+            # will always get the same order independently of the system (they come from a set, which has no deterministic order)\n+            original_dependencies = sorted(original_dependencies, reverse=True)\n             # Sort both list according to the order in their respective file\n             original_dependencies = sorted(original_dependencies, key=lambda x: self.start_lines.get(x, 1e10))\n             merged_dependencies = sorted(merged_dependencies, key=lambda x: self.modular_file_start_lines[x])\n@@ -798,6 +801,9 @@ def compute_relative_order(self, missing_dependencies: set[str]) -> dict[str, in\n                 merged_dependencies.append(dep)\n             else:\n                 original_dependencies.append(dep)\n+        # We need to sort deterministically before actual sorting, so that entries missing (i.e. with value 1e10)\n+        # will always get the same order independently of the system (they come from a set, which has no deterministic order)\n+        original_dependencies = sorted(original_dependencies, reverse=True)\n         # Sort both list according to the order in their respective file\n         original_dependencies = sorted(original_dependencies, key=lambda x: self.start_lines.get(x, 1e10))\n         merged_dependencies = sorted(merged_dependencies, key=lambda x: self.modular_file_start_lines[x])"
        }
    ],
    "stats": {
        "total": 6,
        "additions": 6,
        "deletions": 0
    }
}